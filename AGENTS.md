# Project Notes

本项目用于个人 OJ 网站调试和自动化辅助。

## Development

1. 使用严格 TypeScript。
2. 选择器优先使用稳定 DOM 结构和可见文本，不依赖 Vue 临时 `data-v` 属性。
3. README 命令需要和 `package.json` 保持一致。
4. 不把 API key 写进代码，不提交 `node_modules`。
